// Find Maximum Subarray Sum: Write a function to find the maximum sum of a contiguous subarray (Kadane’s Algorithm).

const maxSubarraySum = (array) => {};

const result = maxSubarraySum([-2, 1, -3, 4, -1, 2, 1, -5, 4]);
console.log(result);
